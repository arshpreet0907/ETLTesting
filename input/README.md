# This folder holds Excel mapping sheets (input to excel_parser.py).
# File naming: <TableName>_mapping.xlsx
# Each file represents one source→target table mapping.
# Once processed by excel_parser.py the sheet can be archived.
