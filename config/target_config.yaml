# config/target_config.yaml
# --------------------------
# Target database connection details.
# Used by connections/target_connection.py
#
# Set 'type' to either 'mysql' or 'snowflake'.
# The matching section below will be used.

type: mysql              # mysql | snowflake

# ── MySQL target ──────────────────────────────────────────────────────────────
# Used when type = mysql
host: localhost
port: 3306
database: etl_output_mysql_target_joins
user: root
password: root
fetchsize: 10000

# ── Snowflake target ──────────────────────────────────────────────────────────
# Used when type = snowflake  (or when --target-mode snowflake is passed)
snowflake:
  account: RPDEFQT-SJ73076
  warehouse: ETL_WH
  database: ETL_OUTPUT_SNOWFLAKE_TARGET_JOINS
  schema: PUBLIC
  role: ACCOUNTADMIN
  user: ARSHPREETSINGH98
  password: '#Yashkijay@123'
